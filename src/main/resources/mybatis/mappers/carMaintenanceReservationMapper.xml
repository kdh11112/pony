<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="kr.co.jhta.pony.dao.ReservationDAO">
  	<insert id="insertCarMaintenanceReservation" parameterType="kr.co.jhta.pony.dto.ReservationDTO">
		INSERT INTO RESERVATION(reservationDueDate, shopNo, memberNo, clientCarNumber, reservationDate, reservationClientRequests)
		VALUES(#{reservationDueDate}, #{shopNo},${memberNo} , #{clientCarNumber}, now(), #{reservationClientRequests})
	</insert>
  </mapper>